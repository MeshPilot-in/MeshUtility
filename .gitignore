# Logs
logs
*.log
*.err.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Node
node_modules
dist
dist-ssr

# Environment files — never commit secrets
*.local
*.env
*.env.*
!.env.example

# Editor directories
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/
graphify-out/

# Bundled Whisper binary (too large for git, distribute separately)
src-tauri/whisper.zip
src-tauri/whisper-extracted/

# Tauri bundled binaries (downloaded at build time)
!src-tauri/bin/whisper/*.exe
!src-tauri/bin/whisper/*.dll
!src-tauri/bin/sherpa/*.exe
!src-tauri/bin/sherpa/*.dll

# Backups and local dumps (contain secrets and local paths)
*backup*.json
*.backup.json

# Local scripts & tools
*.local.ps1
*.local.sh
meshmcp.local.ps1

# Local agent/tooling state & rules
.commandcode/
.meshmemory/
MESH_CHART/
.agents/
.cursor/
.gemini/
.qwen/
.factory/
.kiro/
.grok/
.claude/
AGENTS.md
CLAUDE.md

# MeshSwarm / MeshCrew MCP configs (contain API keys)
opencode.json
.mcp.json
crush.json
.mimo-code.json

# MeshCrew MCP configs (contain API key)
.gemini/settings.json
.qwen/settings.json
.cursor/mcp.json
.kiro/settings/mcp.json
.grok/config.toml
.agents/mcp_config.json
.factory/mcp.json
